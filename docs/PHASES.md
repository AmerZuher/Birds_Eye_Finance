# BirdsEye Finance — Phase Tracker

Update this file's Status lines as you go. Every new chat starts by reading this + CLAUDE.md, not by being re-pasted the full spec.

## Phase 1 — Infra, tokens, primitives, navigation, Settings hub
Status: done — verified on a real Android device/emulator (dev client build)

Scope:
- Scaffold Expo project (TypeScript strict, path aliases), configure NativeWind.
- Drizzle schema + migrations for Expense/Debt/IncomeSource (no Target — CLAUDE.md rule 13).
- MMKV storage wrappers.
- `src/constants/theme.ts` with exactly these four palettes (do not alter without sign-off):

```ts
export const THEMES = {
  platinum: {
    label: { en: 'Platinum', ar: 'بلاتين' },
    ground: '#0c0e11', surface: '#14171c', surfaceAlt: '#1b1f26',
    accent1: '#9fb0c4', accent2: '#e3e8ee', fab: '#9fb0c4', buttonText: '#0d1013',
    glow: { a: '159,176,196', b: '227,232,238' },
  },
  emerald: {
    label: { en: 'Emerald', ar: 'زمرد' },
    ground: '#070f0c', surface: '#0d1a15', surfaceAlt: '#12231c',
    accent1: '#0f9d6c', accent2: '#4fe3ab', fab: '#14b881', buttonText: '#f3fff9',
    glow: { a: '15,157,108', b: '79,227,171' },
  },
  obsidian: {
    label: { en: 'Obsidian', ar: 'أوبسيديان' },
    ground: '#040405', surface: '#08090b', surfaceAlt: '#0e1013',
    accent1: '#3c4fc4', accent2: '#aebdf5', fab: '#3c4fc4', buttonText: '#ffffff',
    glow: { a: '60,79,196', b: '174,189,245' },
  },
  sapphire: {
    label: { en: 'Sapphire', ar: 'سفير' },
    ground: '#060a14', surface: '#0c1426', surfaceAlt: '#111c34',
    accent1: '#2f6fff', accent2: '#00e0e0', fab: '#2f6fff', buttonText: '#ffffff',
    glow: { a: '47,111,255', b: '0,224,224' },
  },
} as const;

export const SEMANTIC = { positive: '#34d399', negative: '#fb7185' };

// Google Fonts via @expo-google-fonts/*: Fraunces (400/600/700), Manrope (400-800), IBM Plex Sans Arabic (400-700).
export const FONTS = {
  display: 'Fraunces_600SemiBold',
  body: 'Manrope_500Medium',
  bodyBold: 'Manrope_700Bold',
  arabic: 'IBMPlexSansArabic_500Medium',
  arabicBold: 'IBMPlexSansArabic_700Bold',
};
```
Default theme on first install: platinum. Radii: cards 22px, sheets 28px top corners, pills 999px, small tiles 11-14px.

- `src/constants/brandIcons.ts` — curated simple-icons subset (CLAUDE.md rule 7).
- All 18 shared UI primitives in `src/components/ui/` (CLAUDE.md rule 4).
- Working tab navigation (Dashboard/Expenses/Debts) with the Android back-button chain (FEATURE_SPEC.md Part 0.2).
- GlassHeader + Navbar/FAB wired to real theme/language state.
- Settings **main hub** built for real (FEATURE_SPEC.md Part 3.2), using the shared primitives.
- About stub (CLAUDE.md rule 16).
- Dashboard/Expenses/Debts as minimal placeholders — navigable, correct chrome, no real feature content.

Definition of Done:
- [x] Boots on an EAS dev client, no red screens. **Verified on a real device** — Android emulator via `npx expo run:android` (dev client, not Expo Go — Expo Go cannot run this app at all, see "Expo Go" note below). Confirmed working after fixing a WSL2/Windows adb bridge issue (see "WSL2 + Android emulator" note below, unrelated to the app itself).
- [x] Cold start shows platinum theme instantly, no flash. Implemented per rule 2 and confirmed on-device.
- [x] All 18 primitives exist, import only from theme.ts (`src/components/ui/*`, barrel at `src/components/ui/index.ts`).
- [x] Drizzle schema matches Expense/Debt/IncomeSource, working migration (`src/db/schema.ts`, generated migration in `src/db/migrations/`, loads correctly through the bundler — see "Metro/Babel gotchas" below).
- [x] brandIcons.ts has a working name → icon lookup — verified via a standalone `tsx` script (`findBrandIcon('Netflix'|'spotify'|'claude')`), not an in-app test render, so there was nothing to delete afterward. 142 icons curated (simple-icons has dropped several major brands — Amazon, OpenAI/ChatGPT, Disney+, Slack, LinkedIn, Adobe, Xbox — presumably to trademark takedowns; unmatched names fall back to generic icons per rule 7 by design).
- [x] Can tap through Dashboard/Expenses/Debts/Settings/About with no crash; Settings is fully real, others are placeholders. **Verified on-device.** First real render surfaced a bug: no screen had an explicit dark background, so every screen fell through to React Navigation's default white container (and the Navbar's blur looked washed-out light instead of dark glass as a result). Fixed by adding `contentStyle`/`sceneStyle: { backgroundColor: theme.ground }` to the root Stack, Settings Stack, and Tabs navigators' `screenOptions`, plus an explicit `backgroundColor: theme.ground` on every placeholder/stub screen's root View and the Settings ScrollView. **Any new screen added in later phases needs this same explicit background — it is not inherited for free.**
- [x] Android back-button chain works even with stub screens. Dashboard-root-swallow implemented via `BackHandler` in `app/_layout.tsx`; Settings/About/sub-screen back relies on Expo Router's native stack pop (automatic). Not yet exercised interactively on-device (only the background-color bug was caught/fixed this session) — worth a deliberate check early in Phase 2.
- [x] ESLint/Prettier/TypeScript all clean — `npm run lint` / `npm run typecheck` / `npx prettier --check .` all pass with zero errors/warnings.

One unresolved loose end from the device test: the header tile/FAB rendered as a vivid saturated blue rather than Platinum's muted `#9fb0c4` — looks like the Sapphire palette. The default-theme code path was checked and is correct (falls back to Platinum when MMKV has nothing stored), so this is most likely leftover MMKV state from an earlier install during that debugging session, not a bug. If it recurs on a clean install, check `ThemeContext.readInitialThemeId()` / the theme grid's `setThemeId` call.

### Environment notes (not app bugs — device/OS specific, but worth knowing)
- **Expo Go cannot run this app.** It only ships Expo's own SDK modules — no `react-native-mmkv` (v4, Nitro-based), `react-native-nitro-modules`, or `react-native-worklets`. Opening it in Expo Go crashes with a generic "app has a bug" dialog. Always use a dev client (`npx expo run:android` / `run:ios`, or an EAS dev build).
- **WSL2 + a Windows-side Android emulator**: if the dev machine is WSL2 with Android Studio/AVDs installed on the Windows side (not inside WSL), `npx expo run:android` looks for devices via the SDK copy of `adb` at `$ANDROID_HOME/platform-tools/adb` specifically. If that's a native Linux `adb` binary, it spins up its own isolated server inside WSL with zero knowledge of the Windows-hosted emulator — `adb devices` from a plain shell can look fine (if e.g. `/usr/bin/adb` happens to be a symlink to the real Windows `adb.exe`) while Expo's own device detection still fails with "No Android connected device found." Fix: make `$ANDROID_HOME/platform-tools/adb` itself a symlink to the Windows `adb.exe` (typically at `/mnt/c/Users/<user>/AppData/Local/Android/Sdk/platform-tools/adb.exe`) so there's only one real adb server in play. Don't bother with `ADB_SERVER_SOCKET` TCP-bridging — WSL2 runs Windows `.exe` files directly through its own interop layer, no networking needed.

### Metro/Babel/toolchain gotchas discovered this phase (read before touching config files)
This project was scaffolded on Expo SDK 57 (RN 0.86, React 19.2, TS 6.0) — noticeably newer than most existing guides/muscle memory. Things that broke and how they were fixed, so nobody rediscovers these the hard way:
- **`react-native-mmkv` v4** dropped the `new MMKV()` class entirely — it's now Nitro-modules-based. Use `createMMKV({ id })` from `react-native-mmkv`, and you must also install the `react-native-nitro-modules` peer dependency explicitly (`npx expo install react-native-nitro-modules`). See `src/lib/mmkv.ts`.
- **`react-native-reanimated` v4** split its Babel plugin's dependency out into a separate `react-native-worklets` package. If bundling fails with `Cannot find module 'react-native-worklets/plugin'`, run `npx expo install react-native-worklets` (it's an implicit peer dep, easy to lose across `npm install` churn since nothing in package.json otherwise requires it).
- **`expo-router`'s `Tabs`** no longer wraps `@react-navigation/bottom-tabs` as an external dependency — it vendors its own fork (there is no `@react-navigation/*` in `node_modules` at all). Import `Tabs`/`BottomTabBarProps` from `'expo-router/js-tabs'`, not the deprecated default `Tabs` export off `'expo-router'`. The custom tab bar goes on `<Tabs tabBar={...}>` directly — **not** inside `screenOptions` (that will fail to typecheck; `tabBar` lives on the navigator config, `screenOptions` is per-screen).
- **NativeWind v4** installs against **Tailwind v3**, not v4 — `npm install nativewind tailwindcss` without pinning pulls Tailwind v4 by default (a completely different CSS-native config model) and doesn't work right with NativeWind 4.x. Pin `tailwindcss@^3.4.4`.
- **Drizzle + expo-sqlite migrations**: `drizzle-kit generate` (dialect `sqlite`, driver `expo`) emits a `migrations.js` that does `import m0000 from './0000_xxx.sql'`. Two things are required for this to bundle: add `.sql` to `metro.config.js`'s `resolver.sourceExts`, **and** add the `babel-plugin-inline-import` Babel plugin configured for `['.sql']` extensions (otherwise Metro tries to parse the raw SQL as JavaScript and fails with "Missing semicolon").
- **`babel-preset-expo`** wasn't hoisted to top-level `node_modules` after a chain of `--legacy-peer-deps` installs (it ended up nested under `node_modules/expo/node_modules/babel-preset-expo`), which broke Babel's preset resolution from `babel.config.js`. Fixed by adding it as an explicit top-level devDependency. If a fresh `npm install` ever un-hoists it again, that's the symptom to look for.
- **ESLint 10** (installed by default via `eslint-config-expo`'s loose `>=8.10` peer range) is incompatible with the `eslint-plugin-react` version currently bundled — it calls the removed `context.getFilename()` API and crashes the linter entirely. Pinned `eslint@^9` in devDependencies as a workaround; revisit if `eslint-config-expo` bumps its bundled `eslint-plugin-react`.
- **TypeScript 6.0** deprecates the `baseUrl` compiler option (needed for the `@/*` path aliases) — add `"ignoreDeprecations": "6.0"` to silence the error until this project migrates off `baseUrl` before TS 7.
- **`.npmrc`** sets `legacy-peer-deps=true` project-wide — `expo-router`'s web preview pulls in `@radix-ui/*`/`vaul` packages with a peer-dependency graph that conflicts with our React 19.2 pin otherwise.
- The React Compiler–oriented ESLint rules (`react-hooks/set-state-in-effect`, `react-hooks/immutability`, `react-hooks/refs`, all new in this `eslint-config-expo`) flag Reanimated's `.value =` mutation pattern and classic `Animated.Value` ref access as violations even though both are the libraries' documented, correct usage. `GlassModal.tsx` has a targeted file-level disable with a comment; `ToggleSwitch.tsx` was rewritten to use `useSharedValue`/`useAnimatedStyle` instead of classic `Animated`, which sidesteps the ref-access rule entirely — prefer that approach over disabling the rule where practical.
- `app.json`: `newArchEnabled` is no longer a valid top-level key on SDK 57 (New Architecture is the only architecture now) — `expo-doctor` will flag it as an unknown schema property.

### Branding
`assets/icon.png`, `splash-icon.png`, `android-icon-foreground.png`, and `favicon.png` are all copies of the user-supplied `AppLogo.png` (project root). Splash/adaptive-icon `backgroundColor` (`#0d1e33`) was sampled from the logo's own background so the composited result reads as one seamless navy panel. No separate Android background/monochrome images were generated (would need real image editing tooling); `adaptiveIcon.backgroundColor` covers the background layer, and the monochrome (Android 13+ themed icon) variant was intentionally left unset rather than shipping an incorrect placeholder.

## Phase 2 — EditProfileScreen + DataScreen
Status: implemented and verified on a real Android device/emulator (dev client via `npx expo run:android`), typecheck/lint/prettier clean
Scope: FEATURE_SPEC.md Parts 3.3 and 3.4 — balance/income CRUD, backup/export/import (file + paste), auto-backup, AI-prompt copy buttons.

New native dependencies added this phase (all via `npx expo install`, requires a dev-client rebuild — `npx expo run:android`): `expo-image-picker`, `expo-clipboard`, `expo-file-system`, `expo-sharing`, `expo-document-picker`. `app.json` gained an `expo-image-picker` plugin entry (photo library permission text) alongside the existing `expo-sharing` plugin auto-added by the installer.

What was built:
- `src/pages/EditProfileScreen.tsx` — avatar upload (expo-image-picker, square crop, stored as base64 data URI on `profile.avatar`), inline name edit, monthly-income line, financial-health badge. Current-balances manager and income-sources manager share one internal `EntryManager` component (add-row + list + footer) since their shape is identical — balances write to `profile.startBalances` via `UserContext.updateProfile` (stamping `lastReconciledDate`), income sources go through new `FinanceContext.addIncomeSource`/`removeIncomeSource` (drizzle insert/delete against the `income_sources` table, live-updating via `useLiveQuery`).
- `src/pages/DataScreen.tsx` — export (builds a full snapshot from the three DB tables + profile/currency/theme, writes it via the new expo-file-system `File`/`Paths` API, shares via expo-sharing), import from file (expo-document-picker, JSON-only) and paste-JSON import, both routed through one `applyImport` → `appendSnapshot` pipeline. Auto-backup panel (Off/Daily/Weekly/Monthly `SegmentedControl`, humanized last-backup line, Backup Now, Restore behind a `ConfirmModal` since it's destructive/replaces all data). AI-prompt copier (expo-clipboard) for the two new prompt files.
- `src/utils/autoBackup.ts` — snapshot build/parse, append (import, ids stripped) vs. replace (restore, full wipe+reinsert) semantics, auto-backup meta/snapshot MMKV read-write, `isBackupDue`, `humanizeLastBackup`.
- `src/prompts/debtsPrompt.ts` / `expensesPrompt.ts` — the copy-paste-to-external-LLM prompt text (CLAUDE.md rule 1's one deliberate exception). Both instruct the LLM to reply with `{ "debts": [...] }` / `{ "expenses": [...] }`, which `parseSnapshot` in autoBackup.ts accepts directly (it tolerantly defaults any missing array/field), so the paste-import box works for both a full backup file and an AI-generated partial JSON without a separate code path.
- `FinanceContext` gained `addIncomeSource`/`removeIncomeSource` plus the derived `effectiveIncome`/`netSavings`/`savingsRate`/`financialHealth` values FEATURE_SPEC Part 5 lists. **Note:** `totalNegativeMonthly` (Phase 3) and `totalExpenses` (Phase 4) don't exist yet, so they're hardcoded to `0` for now with a comment — accurate today (no debt/expense records can exist before those phases), but revisit and wire in for real once Phases 3/4 land, otherwise the financial-health badge will stay stuck at income-only forever.
- `CustomSelect` gained optional `open`/`onOpenChange` (controlled mode) and `hideTrigger` props so it can be driven by an external trigger — used to implement "AmountInput + embedded searchable CustomSelect" (the exact pattern FEATURE_SPEC 1.7/2.7 need for DebtModal/ExpenseModal in Phases 3/4 too). Fully backward compatible — existing uncontrolled call sites (Settings.tsx's language/currency rows) pass neither prop and behave exactly as before.
- `Header.tsx`'s per-route Settings title map: `/settings/edit-profile` now shows "Edit Profile" (`settings.editProfile`) instead of falling back to the generic "Settings" title it had as a Phase 1 placeholder.
- ~20 new translation keys per language under `editProfile.*` and `data.*`, plus `settings.editProfile` and `common.cancel`. EN/AR key sets verified to match exactly.

Definition of Done:
- [x] `npm run typecheck` / `npm run lint` / `npx prettier --check .` all clean.
- [x] EN/AR translation keys verified to match 1:1.
- [x] On-device verification — **Android emulator via `npx expo run:android`** (dev client rebuild required, 5 new native modules). Confirmed working: Settings → Edit Profile → Data navigation and back-chain (hardware back), RTL/Arabic layout throughout both new screens, financial-health badge correctly showing "critical" at zero income, balances/income empty states, auto-backup frequency SegmentedControl (Off→Daily), Backup Now (status line updates to "1 minute ago" live), Restore button correctly gated on frequency≠off AND a snapshot existing, the Restore `ConfirmModal` (including the interpolated `{{time}}` in its subtitle), and — the highest-risk new code path — **Export**, which correctly built `birdseye-backup-2026-09-03.json` via the new expo-file-system `File`/`Paths` API and handed it to the native Android share sheet.
  - One build gotcha hit and fixed this session, worth recording: right after `npx expo install`-ing the 5 new packages, `npx expo run:android`'s bundler threw `Unable to resolve module ./useIncomingShare from expo-sharing/build/index.js` even though that file exists in `node_modules`. Cause was a stale Metro haste-map cache under `/tmp/metro-file-map-expo-*` predating the install. Fix: kill the Metro process, `rm -rf /tmp/metro-cache /tmp/metro-file-map-expo-*`, restart via `npx expo start --clear` (no full native rebuild needed — the dev client APK itself was already fine). If a future phase adds more native deps and hits a similarly nonsensical "module not found" for a file that visibly exists, try this before suspecting the package itself.
  - Not exercised interactively this session (typing into the emulator via `adb shell input text` kept triggering an unrelated OS-level "Try out your stylus" tutorial overlay that ate taps — an emulator/AVD quirk, not an app bug): actually adding a balance/income row end-to-end, paste-JSON import execution, file import via document picker, and the AI-prompt clipboard-copy confirmation state. These reuse the same MMKV/DB/InlineBanner plumbing already proven working by the Backup Now flow above, so risk is low, but give them a real pass (ideally on a physical device, or an AVD with the stylus profile disabled) before calling the phase fully battle-tested.

### Post-Phase-2 polish (from on-device feedback on the built APK)
- **Fixed: launcher icon was cropped.** `assets/android-icon-foreground.png` was a plain copy of the full-bleed `icon.png` (bird's wingtip and wallet corners touching the edges) — Android's adaptive-icon mask (circle on stock/Pixel launchers, squircle on Samsung, etc.) crops anything outside the center ~66% "safe zone", so a meaningful chunk of the artwork was always invisible on real devices. Regenerated `android-icon-foreground.png`: the artwork is scaled to 60% and centered on a synthetic vertical-gradient canvas that matches the original's background gradient (sampled from its corners), so there's no visible seam where the shrunk artwork meets the padding. `icon.png`/`splash-icon.png`/`favicon.png` are untouched (not masked the same way — iOS just rounds corners, and the splash image isn't cropped at all). Regenerating this required `npx expo prebuild --platform android --clean` (the `android/` dir is gitignored/CNG-managed, per `.gitignore`) since `npx expo run:android` alone does **not** re-sync icon assets into an already-generated native project — worth remembering for any future icon/splash tweaks.
- **Fixed: header used a fake "BE" text tile instead of the real logo.** Both `Header.tsx`'s brand tile and `About.tsx`'s logo (CLAUDE.md rule 16 calls for a real logo there, not invented content — this was a Phase 1 placeholder that never got swapped) now render the actual `assets/icon.png` via `<Image>` in a rounded, bordered tile.
- **Improved: header avatar looked bare.** It rendered with no `ring` prop (default `'none'`) and the Settings-active state was a plain 1px white border — inconsistent with the rest of the app's accent-glow visual language. Now always renders with a ring (`'accent'` when Settings is active, `'flat'` otherwise) inside a soft accent-tinted background pill when active, and is very slightly larger (22→24px) for better presence next to the new logo tile.

### Header/Navbar overlay + real glass blur (from on-device feedback)
- **Fixed a real layout bug: GlassHeader and Navbar were laid out in-flow, pushing screen content down, instead of floating over it.** `Header` and `Stack` were plain siblings in a column (`<Header/><Stack/>`), and `Navbar` was a normal (non-absolute) child of the Tabs layout — so there was never any content *behind* either bar for their `BlurView`s to blur; the blur intensity was rendering, just against a flat matching background. New `src/context/ChromeContext.tsx` provider: `GlassHeader` and `Navbar` are now `position: 'absolute'` overlays that report their own rendered height (via `onLayout`) into this context; every screen reads `headerHeight`/`navbarHeight` back out and pads its content to clear them. Updated: `app/_layout.tsx` (Header now renders after Stack so it paints on top; Stack fills the full screen), all 7 screens (Dashboard/Expenses/Debts/Settings/EditProfile/Data/About). Confirmed on-device: content genuinely scrolls underneath both bars now.
- **Fixed: `expo-blur` renders no real blur on Android by default.** Its `blurMethod` prop defaults to `'none'` (a flat translucent `View`) — real blur requires explicitly passing `blurMethod="dimezisBlurViewSdk31Plus"` **and** a `blurTarget` ref (Android has no automatic "blur whatever's behind me" the way iOS's system blur does; the Dimezis BlurView library needs to know which view's live-rendered content to sample). `app/_layout.tsx` now wraps the whole route `<Stack>` in `<BlurTargetView ref={blurTarget}>` (a no-op plain `View` on iOS/web — safe to always render); `ChromeContext` carries that ref so both `GlassHeader`'s and `Navbar`'s `BlurView` can point at it. Confirmed on-device with a real screenshot: scrolling the avatar on Edit Profile behind the header shows its blue tones genuinely blurred through the glass, not just tinted.
  - **False alarm worth recording:** mid-session this looked like it broke touch/scroll (identical screenshots after repeated swipes) and separately looked like it desaturated all UI colors + turned the background pure black. Neither was real. The "broken scroll" was a test artifact — `DataScreen`'s content is only slightly taller than the viewport, so one swipe reaches max scroll and further identical swipes produce identical screenshots; a clean single-swipe before/after comparison proved scrolling works fine. The "desaturated colors/black background" was **coincidental**: `src/constants/theme.ts` (frozen per rule 3) got edited by a different session on this machine mid-test, softening every palette's accent colors — unrelated to the blur code entirely. Moral: when a change looks like it broke rendering, isolate it with a clean before/after on one variable at a time before reverting — the first revert here was premature and had to be undone.
- **Unified the header/navbar glass treatment** — they used different `GLASS` tokens (`headerBorder` vs `navBorder`, different gradient opacities) and only `GlassHeader` had a theme-colored tint layer at all (`Navbar` had none), so per-theme identity only showed in the header and the two surfaces looked like different materials. `GLASS` in `theme.ts` now exposes one shared `border`/`gradientTop`/`gradientBottom`/`blurIntensity`/`tintAlpha` used identically by both components; `tintAlpha` bumped 0.14 → 0.3 so each theme's accent color actually reads through the blur instead of being dominated by the `tint="dark"` BlurView. **Not yet re-verified on-device** — this last round of changes (unified tokens) was made after the user opted to take over on-device verification themselves going forward; typecheck/lint/prettier are clean but the visual result hasn't been screenshotted.
- **Fixed: Bank/Salary rows in EditProfileScreen's balance/income managers had sharp corners**, clashing with the app's consistently-rounded look — they used the shared `ListRow` primitive, which is deliberately flush/hairline-divided for dense continuous lists (correct for the Phase 3/4 transaction lists it's built for) but wrong for these few, spaced-out, individually-removable entries. Replaced with a local rounded card row (matches the `RADII.field` styling already used by the fields directly below it) — `ListRow` itself is untouched.

## Phase 3 — Debts screen
Status: implemented, typecheck/lint/prettier clean, build boots on the Android emulator — full on-device interaction pass still pending (user is doing that testing directly this round, see below)
Scope: FEATURE_SPEC.md Part 1, in full — summary/detail views, grouping engine, WhatsApp integration, DebtModal, ContactsPicker.

What was built:
- `src/context/FinanceContext.tsx` gained the Debt side: a live-queried, newest-first `debts` table read (`orderBy(desc(debts.id))`, which is also what makes "create mode prepends" fall out for free — both in `groupedDebts` transaction order and in which record wins each group's merged contact metadata), `addDebt`/`updateDebt`/`deleteDebt` CRUD, `groupedDebts` (grouping engine per 1.3 — trimmed-name exact match, running base-currency net, type classification, sorted by \|net\| descending, filterable via `debtFilter`/`setDebtFilter` though nothing in the UI surfaces that filter yet, matching spec), and `debtsCalculations` (`totalPositiveAmount`/`totalNegativeAmount`/`totalNegativeMonthly`). `totalNegativeMonthly` now actually feeds `effectiveIncome` (the Phase 2 placeholder `const totalNegativeMonthly = 0` is gone).
- `src/pages/Debts.tsx` — summary view (FlashList, title/subtitle, net-balance stat + the installment-total stat from rule 14 shown only when >0, SearchInput matching name/phone/email/company/notes/date/amount, EmptyState) and detail view (FlashList again — CLAUDE.md rule 6 covers the transaction history too, not just the person list — header block with back button/large Avatar/contact chips/tinted net card, per-transaction rows with an expandable notes+installment panel). Android hardware back closes an open person detail — implemented as a `BackHandler` listener registered only while `selectedGroup` is truthy (RN's back-listener stack is LIFO, so this naturally takes priority over the root layout's Dashboard-swallow listener without any shared registry). Delete uses the shared `ConfirmModal`; because the delete + the person-detail view both derive from the same live `groupedDebts`, deleting a person's last transaction closes the detail view for free (no extra effect needed — see the code comment where an earlier version of this *did* use an effect and tripped the React Compiler's `set-state-in-effect` rule).
- `src/components/DebtModal.tsx` — react-hook-form + zod (rule 5), but driven by `watch()`/`setValue()` rather than `Controller`: every field here is a controlled primitive (AmountInput, CustomSelect, SegmentedControl, plain TextInput), not a ref-forwarding native input, so there's nothing for `Controller` to buy beyond what `setValue` already gives for free. Contact import/unlink (manually editing the name after a contact link clears the link), name auto-suggest from existing debtor names, type toggle, AmountInput's new optional `tint` prop (positive/negative border+label tint, added for this screen), monthly-payment + installment-dates panel gated on type=negative (installment dates only stored when qualifying, per rule 14), single InlineBanner validation. Date validation is local-calendar-component based (`new Date(year, month-1, day)` + round-trip check) — deliberately **not** `toISOString()`-based, which would silently shift dates by a day in any UTC+ timezone (caught during testing: the auto-filled "today" value was failing its own validation).
- `src/components/ContactsPicker.tsx` — full-screen `Modal` (Android back closes it via RN's own `onRequestClose` wiring, no extra code needed), built against SDK 57's newer class-based `expo-contacts` API (`Contact.getAllDetails`/`requestPermissionsAsync`, not the classic `getContactsAsync`). Loading/denied/empty/list states.
- `src/components/BrandGlyph.tsx` — new small shared helper (not one of the 18 primitives, lives in `components/` not `components/ui/`) rendering a curated simple-icons mark by slug via `react-native-svg` path data (rule 7) — used here for the WhatsApp glyph on phone chips/rows, reusable for Phase 4's Quick Add brand icons.
- `src/utils/whatsapp.ts` — `normalizePhone` (spec 1.6's exact rules) + `openWhatsApp` (canOpenURL-guarded deep link).
- `src/context/ChromeContext.tsx` gained `setFabHandler`/`triggerFab` (a ref, not state, so no extra re-renders) — the screen currently on Debts registers its "open create modal" handler on mount; `Navbar`'s FAB now actually calls it instead of the Phase-1 placeholder no-op. Expenses' FAB press is still a no-op until Phase 4 registers a handler too.
- `src/components/ui/GlassModal.tsx` — added the `KeyboardAvoidingView` + internal `ScrollView` that Part 4 of the spec calls for ("keyboard-avoiding on both platforms") but Phase 1/2 never needed since `ConfirmModal`/the Data screen's inline fields didn't require it; DebtModal's ~13-field form is the first real long-form consumer. `ListRow.subtitle` widened from `string` to `string | React.ReactNode` (backward compatible — existing string callers render exactly as before) so the Debts person-row can compose a phone+WhatsApp-glyph+transaction-count line without forking `ListRow`. `Avatar`'s `AvatarRing` union gained a `'settled'` value (spec 1.2 calls for the ring to reflect positive/negative/**settled**, which the existing positive/negative/flat/accent/none set had no slot for).
- ~60 new translation keys under `debts.*`, `debtModal.*`, `contactsPicker.*`, plus `common.add`/`common.delete`. EN/AR key sets verified to match exactly (132 keys each). The old `placeholder.debts.*` keys were removed (Debts is no longer a placeholder; grepped first to confirm nothing else referenced them).

Definition of Done:
- [x] `npm run typecheck` / `npm run lint` / `npx prettier --check .` all clean (one pre-existing-pattern warning remains — react-hook-form's `watch()` triggers a benign "Compilation Skipped: incompatible library" note from the React Compiler linter; not an error, not new to this phase's approach).
- [x] EN/AR translation keys verified to match 1:1 (132/132).
- [x] Build boots on the Android emulator (`npx expo run:android`, no new native dependencies this phase — `expo-contacts` was already installed/configured from earlier phases) — confirmed Dashboard→Debts tab navigation, the FAB opening a real DebtModal, DebtModal's conditional fields (monthly payment/installment dates appearing only for negative-type debts) and its `tint` coloring, and ContactsPicker's permission-granted-but-empty-contacts state, all rendering correctly.
- [ ] Full interactive on-device pass (create/edit/delete a debt end-to-end, person-list→detail navigation and back-button behavior, search filtering, WhatsApp deep link, RTL/Arabic layout) — not yet exercised. Typing into emulator text fields via `adb shell input text` proved unreliable this session (coordinate-scaling mixups between the screenshot preview size and real device pixels, an IME suggestion toolbar that relocated unpredictably, back/ESCAPE keyevents closing the whole modal instead of just the keyboard) badly enough that the user opted to take over on-device testing directly rather than have the agent keep fighting adb taps — see the on-device-testing memory. One real bug **was** caught this way before handoff: the date validator round-tripped through `toISOString()`, which silently shifts a date by a day in any positive-UTC-offset timezone — the auto-filled "today" value was failing its own validation. Fixed to use local Date components exclusively (`todayStr()` and `isValidDateStr()` in `DebtModal.tsx`) — re-verified via typecheck/lint after the fix, but the fix itself hasn't been re-confirmed on-device yet.

## Phase 4 — Expenses screen
Status: implemented, typecheck/lint/prettier clean — on-device interaction pass NOT done this round (see below)
Scope: FEATURE_SPEC.md Part 2, in full — Quick Add autocomplete, filters, ExpenseModal.

What was built:
- `src/context/FinanceContext.tsx` gained the Expense side: a live-queried, newest-first `expenses` table read (`orderBy(desc(expenses.id))`, same "create mode prepends" pattern as Debts), `addExpense`/`updateExpense`/`deleteExpense` CRUD, and `totalExpenses` — every expense's amount normalized to its monthly equivalent via `getMonthlyEquivalent` (any billing period, `src/lib/period.ts`) and converted to base currency, summed (FEATURE_SPEC 0.6/2.2). The Phase 2 placeholder `const totalExpenses = 0` is gone; it now feeds `netSavings`/`savingsRate`/`financialHealth` directly. No expense-specific filter state was added to the context — Expenses' search/category filters live as local `useState` in `Expenses.tsx`, mirroring the pattern Debts actually uses for its own search bar (context only holds `debtFilter`, which nothing in the Debts UI surfaces — the closest analog that's actually wired to a real control is local state).
- `src/pages/Expenses.tsx` — full screen: FlashList (rule 6) with the same header/navbar-clearance padding pattern as Debts via ChromeContext, summary block (title/subtitle/divider + Monthly Total stat), a Quick Add chip row (10 curated services ending in a dashed "Search brands" affordance that opens ExpenseModal directly), SearchInput (name/notes) + category FilterChips (All + the 5 canonical categories), and the expense list (ListRow + a new `ExpenseIconTile` helper) sorted unconfigured-first (amount === 0) then newest-first, with category Badge always shown and period Badge only when amount > 0, an amber "Setup cost" affordance replacing the amount for unconfigured rows, edit/delete IconButtons, and a notes footer when present. Delete goes through the shared ConfirmModal (same as Debts — a hard delete, no soft-delete/history concept for expenses, matching FEATURE_SPEC 2.6's "Confirm removes the expense by id"). FAB handler registered via `ChromeContext.setFabHandler`/`triggerFab`, same as Debts.
- `src/components/ExpenseModal.tsx` — react-hook-form + zod (rule 5), `watch()`/`setValue()` pattern mirroring DebtModal exactly (every field here is a controlled primitive too). Fields: Name (drives the live autocomplete dropdown against the curated services table), Amount + Currency (AmountInput + embedded searchable CustomSelect, new-expense default = base currency), Period (CustomSelect: daily/weekly/monthly/3-6-9 months/yearly/custom, default monthly), Custom days (only when period = custom), Notes (auto-clears a default reminder placeholder — "Remember to fill in this expense's real price." — the instant a real amount is set, tracked so it never clobbers text the user typed themselves), Category chips (the shared `FilterChip` variant of Badge, same 5 categories, default Essential), Icon picker (a horizontal-scroll row of IconTiles: a curated handful of brand icons drawn from the quick-add table plus all 26 generic category icons from the new `src/utils/expenseIcon.ts`; selecting one overrides whatever autocomplete picked). Validation via InlineBanner: name required, amount required — but deliberately accepts `0` as valid (an "unconfigured" placeholder expense is a first-class concept per 2.5's "Setup cost" row, so only a blank/negative/NaN amount blocks save), duplicate name (trimmed lowercase, excluding self in edit) blocks with an explanation. Edit mode patches by id; create mode prepends. **Deviation from the task's literal wording**: instead of a create-mode "sentinel" `id = 0` on the entity itself, Quick Add prefill uses the same `prefill` prop pattern DebtModal already established (`editingExpense: Expense | null` + `prefill?: ExpensePrefill | null`) — `editingExpense` stays `null` for a quick-add, so save naturally falls through to `addExpense` (create mode) without ever needing a sentinel id. This reuses an existing, already-reviewed convention instead of introducing a new one.
- `src/utils/expenseIcon.ts` (new, not a UI primitive — a plain util, like `utils/whatsapp.ts`) — the icon-storage scheme (`icon` column stores `"brand:<slug>"` | `"lucide:<key>"` | `"initial"`), the 5 canonical `ExpenseCategory` values, the 26 generic category icons (lucide-react-native, matching what DebtModal/Debts already import icons from), and `QUICK_ADD_SERVICES` — the curated `{ name, category, defaultPeriod, defaultPriceUSD, icon }` table for 10 common recurring services (Netflix, Spotify, YouTube Premium, Rent, Electricity, Water, Internet, Gym, Phone Plan, iCloud/Cloud Storage), each converting its USD default price to base currency at add-time only (Part 6 — never retroactively).
- `src/components/ExpenseIconTile.tsx` (new, alongside `BrandGlyph.tsx` in `components/`, not `components/ui/`) — resolves a stored icon value to a brand logo, a generic category icon, or an auto-generated colored initial-letter tile, so every expense renders *something* offline and deterministically (rule 7). Shared by both `Expenses.tsx`'s row/quick-add-chip rendering and `ExpenseModal.tsx`'s name-field preview and autocomplete-suggestion rows.
- ~46 new translation keys under `expenses.*` and `expenseModal.*`. EN/AR key sets verified to match exactly (182/182). The old `placeholder.expenses.*` keys were removed (grepped first — nothing else referenced them).

Definition of Done:
- [x] `npm run typecheck` / `npm run lint` / `npx prettier --check .` all clean (the only lint output is the same pre-existing-pattern warning DebtModal already has — react-hook-form's `watch()` triggering a benign "Compilation Skipped: incompatible library" note from the React Compiler linter; not an error).
- [x] EN/AR translation keys verified to match 1:1 (182/182).
- [ ] On-device verification — **not performed this round.** Per the task instructions for this phase, the agent did not launch the Android emulator or drive on-device testing; the user does that themselves. Everything above is verified only via typecheck/lint/prettier, not a real device or emulator build.

Gaps/notes worth a human decision:
- No new one-off UI primitive was introduced — `ExpenseIconTile` and `expenseIcon.ts` are plain composition/util helpers (same tier as `BrandGlyph.tsx`/`whatsapp.ts` from Phase 3), not a 19th shared primitive.
- The Quick Add "lightweight quick-entry field on the Expenses screen itself" that FEATURE_SPEC 2.3 mentions as an alternative to typing inside ExpenseModal was not built as a separate on-screen text field — the quick-chip row plus the dashed "Search brands" affordance (which opens ExpenseModal directly, where the same autocomplete lives) covers the spec's required one-tap and full-search cases without a second, redundant text input duplicating ExpenseModal's own name field.

## Phase 5 — Dashboard (real data)
Status: not started
Scope: not yet specified in FEATURE_SPEC.md — revisit once Debts/Expenses exist and real balance/forecasting requirements are defined.

## Phase 6 — Debts v2 (people, payments, attachments) + global scroll/keyboard fixes
Status: implemented 2026-09-15 (6A–6E), typecheck/lint clean, logic smoke test passing — **not verified on a device**. Needs a dev-client rebuild before testing: new native module `react-native-keyboard-controller`, and `app.json` camera/photo permission text (`npx expo prebuild --platform android --clean`, then `npx expo run:android` — `android/` is CNG-managed). Full design and rationale: `docs/DEBTS_V2_PLAN.md`. `FEATURE_SPEC.md` 0.9 and Part 1 describe this behavior.

Done ahead of the sub-phases:
- `SEMANTIC.warning` (`#fbbf24`) added to `theme.ts` with explicit user sign-off. The Phase 1 token block above predates it (and the later `SEMANTIC` softening to `#10b981`/`#f43f5e`) — `theme.ts` is the source of truth for current values.
- CLAUDE.md rule 3 extended (no hardcoded visual values, no restyled copies of primitives) and now lint-enforced.

What was built:
- **6A — Global UX.** `src/lib/scroll.ts` `HIDDEN_SCROLLBARS` spread on every ScrollView/FlatList/FlashList. `react-native-keyboard-controller` 1.21.9 + `KeyboardProvider` in `app/_layout.tsx`. `GlassModal` lifts itself above the keyboard (animated margin + max height from the live keyboard height) and scrolls the focused field into view; the old `KeyboardAvoidingView` (a no-op on Android, where edge-to-edge stops `adjustResize` from resizing anything) is gone. `EditProfileScreen`/`DataScreen` use `KeyboardAwareScrollView`. Search lists keep taps while the keyboard is open and dismiss it on drag.
- **6B — People.** Migration `0002_people` (table, non-unique `name_key` index, unique `phone_key`/`contact_id` indexes, `debts.person_id`). `PRAGMA foreign_keys = ON` in `src/db/client.ts`. New `DatabaseProvider` runs migrations, then `backfillPeople()` (idempotent; groups exactly like the old trimmed-name engine; merges groups sharing a phone/contact), then the attachment reconcile — rendering waits for it, with the splash still up. `src/lib/people.ts`: name/phone normalization (Arabic letter variants, Arabic-Indic digits), `resolvePerson` (contact → phone → exactly one name match → new), suggestions. `FinanceContext` split: new `DebtsContext` owns people/debts/ledger/attachments and every debt figure; `FinanceContext` keeps income/expenses/savings. `DebtModal` rewritten: phone/email/company fields removed; a person block that binds a `personId` (locked from a person's page; live suggestions with a phone-tail/company disambiguator; one namesake auto-binds visibly with "Not this person? Create new"; several require a pick; a contact whose phone/contact id is known binds its owner). `EditPersonSheet` (details, photo, re-import from Contacts, phone-conflict → one-tap merge, "Merge into…"). `PersonPickerSheet` (move/merge). Person page: edit button and dashed "+ Add phone/email/company" chips.
- **6C — Payments & archiving.** Migration `0003_debt_adjustments`. `src/lib/debtStatus.ts` derives outstanding/paid/progress/status (tolerance 0.005 — decision 2, accepted). Debt rows show `#0042`, then the status badge (Open / Partially paid / Paid) and a progress bar, plus "of {original}". Settled debts leave the active list; History has Settled | Deleted tabs. `DebtDetailSheet` (balance card, notes, installment plan, proofs, ledger, Record payment / Add to debt, Edit, Move) and `AdjustmentSheet` (−/+ with type-aware labels, live "Outstanding after", overpay and delete-would-overpay guards, success haptic + banner on settle; a + adjustment reopens a settled debt). "Settle all" for net-zero people. All debt totals now use outstanding amounts of active debts.
- **6D — Attachments.** Migration `0004_debt_attachments`. `src/lib/attachments.ts` (camera/library/PDF pickers, staging, 2000px JPEG downscale, 15 MB PDF cap, commit/delete ordering, launch reconcile, duplicate-on-append). `src/components/DebtAttachments.tsx` (source sheet, thumbnail strip, pinch-zoom image viewer, PDFs via the share sheet). Proofs can be staged in DebtModal/AdjustmentSheet or added in the detail sheet, optionally tied to an adjustment (📎 in the ledger). Backups (`src/utils/autoBackup.ts`) now carry people/adjustments/attachment records with full id remapping; old-shape backups and AI-prompt imports resolve people through `resolvePerson`; files are never backed up, and the Data screen says so. `app.json` permission text updated.
- **6E — Token cleanup.** Every color literal outside `theme.ts`/`brandIcons.ts` replaced with tokens/`withAlpha()` except About's two support-button tints (open decision, plan §8). One-off primitive radii moved to `RADII`. `Badge` status tones and a chip variant with leading icon/dashed outline; `SecondaryButton` `icon` + `link` variant; `GradientButton` `icon`. Shared `src/components/FormField.tsx` replaces per-form copies (DebtModal, ExpenseModal, new sheets). ESLint `no-restricted-syntax` flags hex/rgb(a) literals and hex-alpha suffixes (at `warn` until About's tints are resolved).

Follow-up from the first on-device look (2026-09-15):
- "I paid / Add to debt" and "I owe / Owed to me" now share `SegmentedControl size="large"` (icons + semantic tints) — DebtModal's hand-built toggle is gone.
- Attachments moved from a bare thumbnail strip to a Proofs card → Proofs sheet → preview sheet, with a real "Open with…" (`expo-intent-launcher` ACTION_VIEW on Android — new native module, rebuild needed — and the share sheet on iOS) plus Share and Remove. Editing an adjustment shows that entry's own saved proofs.
- Edit Person's email/company placeholders were falling back to the platform's default (near-invisible on dark themes). Fixed at the root: new shared `TextField` owns placeholder/cursor/selection colors; all form sheets use it (CLAUDE.md rule 5).
- The person row's chevron had no action; it now opens the person like the row does.
- Second on-device look: an attached image showed a blank thumbnail and "Media not found" when opened. Root cause: `File.move`/`File.copy` are **async** in expo-file-system SDK 57 (`moveSync`/`copySync` are the blocking ones), and the attachment code didn't await them — rows were written pointing at `attachments/<name>` while the file was still in flight (or never arrived). Every move/copy is now awaited, `addAttachments` resolves only after each file lands, backup-append copies files before its transaction, and the same bug in `savePersonAvatar` is fixed. Attachments saved with the broken build have no file; the launch reconcile drops those rows, so they need re-adding.
- Proofs card no longer shows inline previews (the Proofs sheet lists everything); rows use a file-type icon tile (image / PDF) instead of a thumbnail, and machine-generated picker names show as "Photo" / "PDF document".

Deviations from the plan, and why:
- Attachments are stored flat (`attachments/<uuid>.<ext>`), not in per-debt folders — debt ids are remapped on restore, so an id-named folder could end up holding another debt's files.
- The focused-input scroll in `GlassModal` measures against the sheet's own scroll view instead of using `useReanimatedFocusedInput()`'s layout, which is window-relative and goes stale as the sheet lifts.
- A backup's own `people` records merge into existing people only by phone/contact id (never by name), so two same-name people in a backup stay two.
- Visible shade change: InlineBanner, Avatar rings, ConfirmModal and the debt tiles were using stale copies of the old semantic green/red; they now use current `SEMANTIC` values.

Definition of Done:
- [x] `npx tsc --noEmit` clean.
- [x] `npx eslint src app`: 0 errors, color rule at `error`. 5 warnings — 4 react-hook-form `watch()` "Compilation Skipped" notes (accepted pattern), 1 pre-existing unused variable (`Analytics.tsx` `topExpenses`).
- [x] Prettier: every file changed in this phase passes `--check`. Repo-wide `npx prettier --check .` still reports 5 files this phase didn't touch (README.md, Header.tsx, DonutChart.tsx, MoneyAmount.tsx, CurrencyContext.tsx) — pre-existing.
- [x] EN/AR translation keys 1:1 (329/329); no literal `t('…')` key missing.
- [x] Logic smoke test (identity resolver incl. Arabic variants, phone precedence, ambiguity; debt status math) passes via `tsx` — kept outside the repo (no test runner is set up).
- [ ] On-device pass (dev client; Android edge-to-edge + iOS): keyboard over DebtModal's last field, CustomSelect search, Edit Profile; no scrollbars anywhere; person binding (plan §3.5 table); payment → Partially paid → Paid → History → reopen; Settle all; merge and move; attachments from camera/library/PDF, viewer, delete; backup export → import (append) → restore (replace).
- [ ] Backfill run against a copy of real pre-Phase-6 data: every person group unchanged, except groups intentionally merged because they share a phone or contact.
- [x] About's support-button tints moved to `SUPPORT_TINTS` in `theme.ts` (user sign-off 2026-09-16); the lint rule is now `error`.

## Release 2.0.1 (prep, 2026-09-16)
Status: code/config done, typecheck/lint clean, prebuild re-run and checked — on-device check pending.

Release steps (full detail in `docs/DEVELOPMENT.md` → Release builds):
1. `npx expo prebuild --platform android --clean` — the version, splash drawables and permissions only reach the generated `android/` through prebuild.
2. `npx expo run:android --variant release`.
3. Upload `apk/birdsEyeFinance_V2.0.1.apk` to a new GitHub Release.

- **Lost and restored: the release-APK copy step.** It used to be a hand edit in the generated, gitignored `android/app/build.gradle` (rename to `birdsEyeFinance_V<versionName>.apk`, copy to `apk/`). The `prebuild --clean` needed for this release wiped it. It now lives in a local config plugin, `plugins/withReleaseApk.js` (registered in `app.json`), which re-adds the Gradle task on every prebuild. The build output keeps Android's default `app-release.apk` so `expo run:android --variant release` can still install it; the renamed copy lands in `apk/`. Also note: a non-interactive `expo prebuild` clears `android/` on its own if it considers the project malformed — another reason nothing may be hand-edited there.
- Release signing is unchanged: the regenerated `android/app/debug.keystore` is React Native's standard debug key (SHA-256 `FA:C6:17:45:…:03:3B:9C`), the same one earlier releases used, so 2.0.1 installs over 2.0.0.
- Version `2.0.0` → `2.0.1` (`app.json` `expo.version`, shown on About via `expo-constants`); `android.versionCode` `1` → `2` — the Play Store rejects an upload that doesn't increase it, and installers compare it for updates.
- Splash logo was cropped top and bottom on Android 12+. The system shows expo-splash-screen's 288dp canvas through a 192dp circle, and the logo was drawn at `imageWidth` 220dp, which put the wingtip and wallet corner 102dp from centre (mask radius 96dp). `imageWidth` → 190dp: simulating the mask on the real asset gives 0 artwork pixels outside the circle (farthest 88dp), vs 174 sampled pixels cut before. iOS and pre-12 Android show the logo about 14% smaller.
- About page, look unchanged: support-button colors are `SUPPORT_TINTS` tokens holding the exact values it used; its buttons are `SecondaryButton variant="pill"` (accent / tinted / neutral tones reproduce the previous pills value-for-value; new `leading`/`trailingIcon` slots for the GitHub repo button); the logo glow radius is the new `RADII.logoTile` (25, as before). No hardcoded colors remain anywhere, and ESLint now errors on new ones.
- [ ] After prebuild, on a device: splash logo fully visible on Android 12+; About shows "Version 2.0.1"; About looks exactly as before on a dark and a light theme.
- Published 2026-09-15. The GitHub Release was first created under tag/title `v2.1.0` while its APK is 2.0.1 (versionName `2.0.1`, versionCode `2`); the user chose to rename it to `v2.0.1` rather than rebuild. The release rule now requires the tag to be exactly `v<version>` (CLAUDE.md rule 17, DEVELOPMENT.md → Release builds).

## Release 2.1.0 (2026-09-15)
Version `2.0.1` → `2.1.0` (`app.json` `expo.version`), `android.versionCode` `2` → `3`. The user commits, pushes and publishes the GitHub Release (tag and title `v2.1.0`); no `v2.1.0` tag existed when the version was bumped. Build with `npm run release:android` (prebuild + release build), then upload `apk/birdsEyeFinance_V2.1.0.apk`. The on-device checklists below are the user's to tick.

- About → Website now opens `https://amer-alreyahi.vercel.app/` (was the GitHub profile). Changed after the 2.0.1 APK was built, so it isn't in 2.0.1.

### 2.1.0 build (2026-09-15)
Status: typecheck/lint/Prettier/translation parity checked (results below) — **not verified on a device**. Needs a native rebuild before testing — **with a prebuild**, not just `npm run android`: new native module `expo-navigation-bar` and its plugin in `app.json` (`npm run prebuild:android`, then `npm run android`). The first device test ran on a debug build without the prebuild, so the navigation-bar plugin wasn't applied.

- **Live exchange rates** (FEATURE_SPEC 0.5; CLAUDE.md rule 1 gained an exchange-rate exception, user-approved). `src/lib/exchangeRates.ts`: ExchangeRate-API open access → currency-api on jsDelivr → currency-api on Cloudflare, 6 s timeout each. Responses are zod-validated and inverted (providers quote units per SAR; the app stores SAR per unit). A response is accepted only when all 20 original currencies are present and every quote is positive; it is cached in MMKV with time and source, merged so a currency a download lacks keeps its last rate. Checked on launch and on return to the foreground; downloads only when rates are missing or ≥ 24 h old, with at most one automatic retry per 30 min. `convertToBase` uses downloaded rates, else the built-in table. Settings: "Live exchange rates" toggle (on by default), status line, Refresh with a result banner, and the required "Rates By Exchange Rate API" link.
- **Every currency.** 153 ISO 4217 currencies — every one both providers quote, minus ANG, HRK, SLL, XDR and ZWL (replaced or not holdable) — generated by `npm run currencies` (`scripts/buildCurrencies.ts`) into `src/constants/currencyTable.ts`, with English/Arabic names and symbols from ICU and built-in rates from 2026-09-15 (replacing the stale static table: EUR was 4.07 vs 4.33 live, TRY 0.108 vs 0.077). The original 20 keep their hand-picked names and symbols and come first. Every currency goes through the same pickers, conversion and formatting. The unused `locale` field is gone. `CustomSelect` options gained an optional `description` that search also matches, so currency pickers show and search by name (`CurrencyContext.currencyOptions`); their search placeholder now reads "Search by code or name" (was "Base Currency").
- **Data screen: export and import only.** The Auto-Backup card, Restore and its confirmation, and the backup-due check are gone. `src/utils/autoBackup.ts` → `src/utils/dataTransfer.ts`, without the frequency/meta/snapshot helpers or `replaceWithSnapshot`. The stored snapshot and meta — and the old font-size value — are deleted at launch (`removeRetiredKeys`). Imports now ask first: parse → count what would be added → `ConfirmModal` ("Adds … on top of your current data — nothing is replaced …") → append. **Behavior change:** an import's profile no longer overwrites yours — its balances are appended, and its name and photo only fill a profile that has none (the photo only if it exists on this device). `ConfirmModal` gained `icon` and `tone="accent"`; its default look is unchanged. 17 `data.autoBackup.*` / `data.lastBackup.*` keys removed per language. The Settings row keeps its "Backup & Data" label.
- **Edit Person photo.** The cause is not confirmed with a logcat yet. Fixed the three differences from Edit Profile's picker: permission is requested first (denial shows a banner), `quality: 0.7`, and destroyed-Activity recovery. Both screens now pick through `pickAvatarPhoto` (`src/lib/avatars.ts`), which remembers who the photo is for while the picker is open. `PendingPhotoRecovery` (root layout) delivers a recovered photo: the profile's is applied and Edit Profile opens; a person's goes to Debts, which reopens that person's Edit Person sheet with it (kept only on Save). Failures are logged with `console.warn` and shown in the screen's banner.
- **Profile photo stored relative.** Saved as `app-avatar:<file>` under `avatars/`, like person photos; Header, Settings and Edit Profile display it through `avatarDisplayUri`. Existing absolute `profile-avatar-*.jpg` photos are moved into `avatars/` at launch, found by file name, so a changed iOS container path doesn't matter.
- **Honor / MagicOS grey band over the tab bar.** `expo-navigation-bar` plugin with `enforceContrast: false`, so the system draws no contrast layer behind the transparent navigation bar. Navigation-bar and status-bar icons now follow the theme — dark on light themes; the status bar was always light before, including on light themes. (`expo-system-ui` was also added here to clear prebuild's `userInterfaceStyle` warning, then removed after the first device test — see the follow-up below.)
- **Font size setting removed.** It was a mock — `fontScaleMultiplier` was never read. Removed the Settings row, `ThemeContext`'s font-scale state, `src/constants/fontScale.ts`, the MMKV key and `settings.fontSize`. Text still follows the system font size. CLAUDE.md rule 12 reworded (user-approved).
- **Currency button.** `AmountInput`'s currency is now a pill button in the same position — tinted fill and border, code + chevron, pressed state, `hitSlop` for a comfortable touch target, and a "Change currency (SAR)" accessibility label. A fixed currency (the adjustment sheet) is plain text with no chevron.
- Settings rows use `IconTile` instead of four hand-built copies of the same tile (same size, radius and wash).
- Hygiene: unused `topExpenses` removed from Analytics; the 5 files that failed repo-wide Prettier are formatted.
- Docs: CLAUDE.md rules 1, 4 and 12; FEATURE_SPEC 0.1, 0.5, 0.7, 1.7, 1.11, 2.7, 3.2, 3.3, 3.4 and Part 6; DEVELOPMENT.md; README; ENHANCEMENT_PLAN §8 (the auto-backup removal ships in 2.1.0, not 2.0.2).

On-device checks (after the rebuild):
- [ ] Exchange rates: online first launch → Settings shows "Updated <today> · ExchangeRate-API" and non-SAR totals reflect it; airplane mode → Refresh shows the error banner and the status line doesn't change; toggle off → note shown, Refresh hidden; relaunch offline → same rates still in use.
- [ ] Currencies: search the picker by code, English name and Arabic name; add a debt in a new currency (e.g. KES) and pick a new base currency (e.g. MAD) — amounts and symbols look right in English and Arabic.
- [ ] Currency pill in DebtModal (both type tints), ExpenseModal and Edit Profile's balance/income rows; plain text in the adjustment sheet; position correct in Arabic.
- [ ] Data screen: no Auto-Backup card; Export; Import from file and from paste → confirmation with counts → Cancel changes nothing, Import adds; invalid JSON → error with no confirmation; after import, your name and photo are unchanged and the file's balances are added.
- [ ] Edit Person photo on the **release** build: Change photo → pick → crop → shows in the sheet → Save → shows on the person page and list; deny photo permission → banner. With Developer options → "Don't keep activities" on: the photo comes back in the reopened Edit Person sheet (and, from Edit Profile, on Edit Profile). Turn the option off afterwards.
- [ ] Profile photo: an existing one still shows after upgrading (Header, Settings, Edit Profile); changing it works.
- [ ] Honor (gesture navigation): no grey band under the tab bar, on a dark and a light theme; status-bar and gesture-handle contrast on a light theme; a non-Honor phone looks unchanged.
- [ ] Settings: no font-size row; Appearance ends with Language.

### 2.1.0 follow-up — first device test (2026-09-15)
Tested on a debug build made with `npm run android` without a prebuild.
- **Edit Person photo — root cause found.** After the picker returned, the sheet was invisible over the person page (offscreen, backdrop transparent) and popped back on the next touch; a tap on that invisible backdrop closed it and discarded the photo — the original "Change photo does nothing" report. `GlassModal` now rests open on plain React styles (`settled`) and animates on the UI thread only while it travels (DEVELOPMENT.md, gotcha 8). Every sheet gets this, including proofs picked from inside DebtModal or the adjustment sheet.
- **Currency button rendered unstyled** (no fill or border, chevron wrapped under the code): NativeWind's JSX transform drops `Pressable` function styles. It now uses a static style with pressed state in React, as a 30pt-high pill (gotcha 7).
- **Header lost its glass** while the tab bar and sheets kept theirs. No header, glass, theme or color code changed; the only native differences were the two new modules. `expo-system-ui` was forcing light mode in that build (its bundled default, since no prebuild ran) and overrides day/night mode app-wide in any build, so it's removed (gotcha 9). **Not confirmed as the cause** — re-check after a prebuild.
- [ ] After `npm run prebuild:android` then `npm run android`: header glassy on Dashboard, Analytics, Expenses and Debts. If not: does it recover after opening Settings and coming back, and does a release build show it?
- [ ] Edit Person → Change photo → pick: the sheet stays visible with the new photo; Save keeps it; closing without Save discards it. Same for a proof added from inside DebtModal and the adjustment sheet.
- [ ] Currency pill in DebtModal (both type tints), ExpenseModal and Edit Profile; the pressed state is visible.

### 2.1.0 follow-up 2 — payments in another currency, Settings layout (2026-09-15)
- **Record payment / Add to debt in another currency** (FEATURE_SPEC 1.10). The adjustment sheet's amount has the currency button, starting on the debt's own currency. A foreign amount is converted into the debt's currency with the rates in use (a live "≈ … in the debt's currency" line) and stored converted, so status and every total work as before. The typed amount and currency are kept too — migration `0005_adjustment_entered_currency` adds two nullable columns to `debt_adjustments` in place — and lead the ledger row's subtitle. Editing keeps the recorded conversion unless the amount or currency changes. Paying the full balance in another currency settles the debt: within one cent of the entered currency counts as exactly the outstanding. The overpay message also gives the outstanding in the entered currency. Exports carry the new fields; older files import as before. `CurrencyContext` gained `convert(amount, from, to)`.
- **Settings layout** (FEATURE_SPEC 3.2). A **Themes** card with one title and a Dark/Light SegmentedControl (opening on the current theme's group) replaces "Appearance" + "Dark Themes" + "Light Themes" stacked headings. A **General** card holds Language, Base currency, Live exchange rates, Backup & Data and Notifications. `SettingsRow` now owns its accent icon tile (32pt), has roomier rows, an optional subtitle, and a footer lined up with the label.
- **Live exchange rates row redesigned.** The status is the row's subtitle, in relative time ("Updated today, 15:42 · ExchangeRate-API", "Built-in rates · 3 days ago", "Off · rates from yesterday"). Under the label sit a discreet attribution link and a small Refresh chip whose icon turns while downloading. A successful refresh just updates the subtitle; only a failure shows a banner.
- [ ] Another currency: on a SAR debt, record 100 USD → the "≈ 375.00 SAR…" line; the ledger row reads "−375.00 SAR" over "100.00 USD · date"; paying the full balance in USD settles it; overpaying in USD shows both currencies; editing that entry without changes keeps its amount.
- [ ] Settings in English and Arabic: the Dark/Light switch and tiles, the General rows, and the rates row on, off, refreshing, and failing offline.
- [ ] Upgrade over 2.0.1: existing payments still show and count (migration 0005 runs at launch).

### 2.1.0 follow-up 3 — light themes, rates panel, contacts picker (2026-09-15)
- **Four new light themes** (`theme.ts`, at the user's request): Blush (berry pink), Coastal (ocean blue), Matcha (olive green) and Graphite (neutral charcoal) — each a hue family no light theme had — so Settings shows 8 dark and 8 light.
- **Live exchange rates, third pass.** The row's subtitle now explains the feature; the state lives in a recessed status panel under it: a status dot (green: live and ≤ 2 days old; amber: built-in or older; muted: paused), "Updated today, 15:42"-style text, a small muted attribution link, and a round tinted refresh button that spins while downloading. New shared pieces: `IconButton`'s `spinning` prop and `SecondaryButton`'s `caption` variant; the Refresh chip and its local spinner are gone.
- **Contacts picker.** It already used the shared primitives (GlassHeader, SearchInput, ListCard, ListRow, Avatar, EmptyState). Most likely what looked foreign: its glass header blurred the screen *underneath* the picker rather than the contact list, and the list never scrolled under the header anyway. The picker now wraps its content in its own `BlurTargetView` (passed through `GlassHeader`'s new `blurTarget` prop), the search field and contacts scroll under the glass like every screen, and loading is an `EmptyState` instead of bare text. Needs the user's eyes to confirm this was the issue.
- [ ] Settings → Themes → Light: Blush, Coastal, Matcha and Graphite on Dashboard, Debts, a sheet and Settings — text, icons, toggles and tab bar all readable.
- [ ] Rates panel: fresh (green), built-in/old (amber), paused (muted, no refresh button), refreshing (spinning), offline refresh (error banner); the attribution link opens the site; Arabic layout.
- [ ] Contacts picker (DebtModal → Import from Contacts; Edit Person → Re-import): loading and permission-denied states look like the rest of the app.

### 2.1.0 follow-up 4 — contacts glass, themed secondary buttons, rates panel width (2026-09-15)
- **Contacts picker glass restored.** Its own `BlurTargetView` (follow-up 3) made the header a flat grey panel on the device, so it's back on the app's shared blur target — the setup that looked glassy before — and `GlassHeader`'s `blurTarget` prop is removed again (DEVELOPMENT.md, gotcha 5). The search field as the list header and the `EmptyState` loading state stay.
- **"Import from Contacts" didn't follow the theme** because it's `SecondaryButton`'s neutral outline, which was a colorless white wash and hairline — flat grey on every colored theme. The neutral outline now uses the theme's glow color (wash 0.10, border 0.28 — IconButton's tinted material) with the icon in `accent2`; the label stays `textPrimary`. This changes every neutral outline button: Import from Contacts, Re-import from Contacts, Merge with…, Import from File, ConfirmModal's Cancel, the proofs sheets' buttons and one on the Debts screen.
- **Rates status panel spans the card.** `SettingsRow`'s footer is full-width now, so the panel keeps the same gap to the General card's edge on both sides.
- [ ] Contacts picker header is glassy again.
- [ ] Neutral outline buttons on a dark theme, a light theme and Graphite: tinted, readable, and Cancel still reads as the quieter choice beside a gradient button.
- [ ] Rates panel: equal left and right gaps to the card edge, in English and Arabic.

Note: `docs/ENHANCEMENT_PLAN.md` §7 proposes its own "Phase 6–9" numbering. That plan is not approved yet; renumber it after this phase when it is.
